# SigNoz MCP investigation (Track 3)

Fixed prompt for agent-native rehearsal (do not claim causation):

```text
For service `lms-backend` version `<bad-sha>`, environment `hackathon-demo`, compare p95 latency and error rate on route `/api/v1/internal/home/overview` for the configured 90 seconds before versus after the deployment marker, and return the three slowest traces. Report temporal and version correlation only; do not claim the commit caused the change.
```

## Prerequisites

- SigNoz MCP running at `http://localhost:8000/mcp`
- `SIGNOZ_API_KEY` configured for direct API validation
- Incident window generated by `scripts/demo-regression.sh`

## Validate output

Save sanitized MCP JSON to `test/fixtures/signoz/mcp-investigation.json` and run:

```bash
node scripts/verify-mcp-result.mjs
```

The validator checks service, SHA, route, latency/error fields, three trace IDs, and rejects causal wording.
