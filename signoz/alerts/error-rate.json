{
  "schemaVersion": "v1",
  "alert": "GreenLight — candidate error rate regression",
  "alertType": "TRACES_BASED_ALERT",
  "ruleType": "threshold_rule",
  "evalWindow": "5m",
  "frequency": "1m",
  "condition": {
    "compositeQuery": {
      "schemaVersion": "v1",
      "queryType": "builder",
      "queries": [
        {
          "type": "builder_query",
          "spec": {
            "name": "A",
            "signal": "traces",
            "disabled": false,
            "aggregations": [
              {
                "expression": "count()"
              }
            ],
            "filter": {
              "expression": "service.name = $service AND service.version = $version AND deployment.environment.name = $environment AND http.route = $route"
            }
          }
        }
      ]
    },
    "op": ">",
    "target": 5,
    "matchType": "1",
    "selectedQueryName": "A",
    "targetUnit": ""
  },
  "labels": {
    "severity": "critical",
    "owner": "greenlight"
  },
  "annotations": {
    "summary": "Candidate service.version produced error spans above the configured absolute floor on the evaluated route.",
    "description": "Deployment correlation is evidence of temporal and version association, not proof of causation. Open the GreenLight change receipt for the verified evidence chain."
  },
  "preferredChannels": []
}
